#include <stdio.h>

int main()
{
    char name;
    int age;
    printf("enter the name of the person");
    scanf("%s", name);
    printf("enter the age of the person");
    scanf("%d", age)
        printf("enter the %s enter the %d.\n", name, age);
    return 0;
}
