#include <stdio.h> int main() { int a[50], n, i, num, count = 0; printf("Enter size: "); scanf("%d", &n); printf("Enter numbers:\n"); for(i = 0; i < n; i++) { scanf("%d", &a[i]); } printf("Enter number to find frequency: "); scanf("%d", &num); for(i = 0; i < n; i++) { if(a[i] == num) count++; } printf("Frequency of %d is: %d\n", num, count); return 0; }
